import ReactDOM from "react-dom/client";
import App from "./App.tsx";
import "/src/index.css"; // Ensure the CSS file is imported


ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
